# gestao-usuarios
Sistema CRUD básico p/ gestão de usuários usando Flask e manipulando arquivos JSON locais

# Setup
```pip install -r requirements.txt```

# Run
```python main.py```

# Acesso
```http://localhost:5000``` ou o que for indicado no terminal.